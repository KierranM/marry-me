require 'spec_helper'

describe Marry::Me do
  it 'has a version number' do
    expect(Marry::Me::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
